export const QuestionCSV = ({
  treatmentId,
  questionId,
  derivedFromQuestionId,
  sequenceId,
  derivedSequenceId,
  amountEarlier,
  amountLater,
  timeEarlier,
  timeLater,
  maxTime,
  maxAmount,
  horizontalPixels,
  verticalPixels,
  shownTimestamp,
  choiceTimestamp,
  choiceTimeSec,
  viewType,
  choice,
}) => {
  return {
    treatmentId,
    questionId,
    derivedFromQuestionId,
    sequenceId,
    derivedSequenceId,
    amountEarlier,
    amountLater,
    timeEarlier,
    timeLater,
    maxTime,
    maxAmount,
    horizontalPixels,
    verticalPixels,
    shownTimestamp,
    choiceTimestamp,
    choiceTimeSec,
    viewType,
    choice,
  };
};
