{
  "name": "@the-discounters/firebase-test-shared",
  "description": "Firebase test library",
  "version": "1.0.0",
  "main": "index.js",
  "type": "module",
  "private": true,
  "scripts": {
    "lint": "eslint .",
    "build": "echo 'Build script currently does nothing in @the-discounters/firebase-shared module.'",
    "shell": "firebase functions:shell",
    "start": "yarn run shell",
    "test": "mocha './src/**/*.test.js' --timeout 5s",
    "test:watch": "mocha './src/**/*.test.js' --watch --parallel --timeout 5s"
  },
  "dependencies": {
    "firebase": "^10.6.0",
    "firebase-admin": "^11.8.0"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^3.0.1",
    "@firebase/testing": "^0.20.11",
    "eslint": "^8.15.0",
    "eslint-config-google": "^0.14.0",
    "jest": "^29.7.0",
    "mocha": "^10.2.0"
  }
}
