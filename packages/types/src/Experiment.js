export const Experiment = ({
  path,
  experiment_id,
  treatment_question_id,
  status,
  num_participants_started,
  num_participants_completed,
  type,
  latin_square,
  start_date,
  end_date,
  num_participants,
  prolific_code,
  prolific_study_id,
  description,
  treatmentQuestions,
}) =>
  Object.freeze({
    path,
    experiment_id,
    treatment_question_id,
    status,
    num_participants_started,
    num_participants_completed,
    type,
    latin_square,
    start_date,
    end_date,
    num_participants,
    prolific_code,
    prolific_study_id,
    description,
    treatmentQuestions,
  });
