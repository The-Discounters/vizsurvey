import { AmountType } from "./AmountType.js";

export function SurveyQuestion({
  treatmentQuestionId,
  treatmentId,
  questionId,
  sequenceId,
  viewType,
  interaction,
  variableAmount,
  amountEarlier,
  timeEarlier,
  dateEarlier,
  amountLater,
  timeLater,
  dateLater,
  maxAmount,
  maxTime,
  verticalPixels,
  horizontalPixels,
  leftMarginWidthIn,
  bottomMarginHeightIn,
  graphWidthIn,
  graphHeightIn,
  widthIn,
  heightIn,
  showMinorTicks,
  instructionGifPrefix,
  comment,
  shownTimestamp,
  dragAmount,
  choice,
  choiceTimestamp,
  choiceTimeSec,
}) {
  return {
    treatmentQuestionId,
    treatmentId,
    questionId,
    sequenceId,
    viewType,
    interaction,
    variableAmount,
    amountEarlier,
    timeEarlier,
    dateEarlier,
    amountLater,
    timeLater,
    dateLater,
    maxAmount,
    maxTime,
    verticalPixels,
    horizontalPixels,
    leftMarginWidthIn,
    bottomMarginHeightIn,
    graphWidthIn,
    graphHeightIn,
    widthIn,
    heightIn,
    showMinorTicks,
    instructionGifPrefix,
    comment,
    shownTimestamp,
    dragAmount,
    choice: choice ? choice : AmountType.none,
    choiceTimestamp,
    choiceTimeSec,
  };
}
