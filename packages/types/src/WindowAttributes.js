export const WindowAttributes = ({
  devicePixelRatio,
  innerHeight,
  innerWidth,
  outerHeight,
  outerWidth,
  screenLeft,
  screenTop,
}) =>
  Object.freeze({
    devicePixelRatio,
    innerHeight,
    innerWidth,
    outerHeight,
    outerWidth,
    screenLeft,
    screenTop,
  });
