export const TreatmentQuestion = ({
  treatmentQuestionId,
  experimentId,
  sequenceId,
  treatmentId,
  questionId,
  instructionQuestion,
}) =>
  Object.freeze({
    treatmentQuestionId,
    experimentId,
    sequenceId,
    treatmentId,
    questionId,
    instructionQuestion,
  });
