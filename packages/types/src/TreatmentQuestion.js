export const TreatmentQuestion = ({
  path,
  exp_id,
  id,
  question_id,
  sequence_id,
  treatment_id,
}) =>
  Object.freeze({
    path,
    exp_id,
    id,
    question_id,
    sequence_id,
    treatment_id,
  });
