export const TreatmentQuestion = ({
  path,
  exp_id,
  id,
  question_id,
  sequence_id,
  treatment_id,
  instruction_question,
}) =>
  Object.freeze({
    path,
    exp_id,
    id,
    question_id,
    sequence_id,
    treatment_id,
    instruction_question,
  });
