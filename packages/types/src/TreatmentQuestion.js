export const TreatmentQuestion = ({
  treatment_question_id,
  experiment_id,
  sequence_id,
  treatment_id,
  question_id,
  instruction_question,
}) =>
  Object.freeze({
    treatment_question_id,
    experiment_id,
    sequence_id,
    treatment_id,
    question_id,
    instruction_question,
  });
